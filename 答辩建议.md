# EchoSQL（MiniDB）答辩建议

> 基于项目实际代码、测试与文档（README / grammar.md / SRS / 测试文档 / git 历史）整理。
> 项目现状：481 个测试全部通过；核心模块覆盖率 >90%；黑盒用例 253 个。

---

## 一、技术选型（怎么讲、为什么）

答辩定位一句话：**"用 Python 标准库手写一个教学型关系型数据库，贯通 SQL → 执行计划 → 数据页的完整链路"**，强调"理解原理、不调库"。

| 层级 | 选型 | 讲法要点 |
| --- | --- | --- |
| 语言 | Python 3.9 | 核心功能（编译器、存储）只用标准库，`struct` 做二进制编解码，展示对底层原理的理解而非依赖第三方框架 |
| 编译器 | **手写递归下降**（Lexer/Parser/Semantic/Planner 四阶段） | 不用 PLY/antlr，能逐层讲清词法→语法→语义→计划，这是最容易被追问也最加分的部分 |
| 存储 | 4KB 页 + **槽页（Slotted Page）** + LRU/FIFO 缓存 + Checkpoint | 对标数据库教材经典结构（对应 SRS 第 6 章），证明不是玩具实现 |
| Web | Flask（REST API）+ React/Ant Design 前端 | 满足 SRS 2.3"也可通过 API 调用"；前端用现成 antd 组件，不重复造轮子 |
| 测试 | pytest + pytest-cov，分层用例 | 单测/集成/E2E/边界/黑盒，量化验收 |

**选型理由话术**：分层解耦——`sql_compiler` / `storage` / `engine` 三个模块相互独立、可单独测试；编译器产出逻辑计划、引擎调用存储层，链路清晰（呼应 README 中的架构图）。

---

## 二、代码抽检（老师可能抽查哪里，你要能讲什么）

以下代码点已实际核查，都是被抽检概率高的位置，务必能当面讲清：

1. **`storage/buffer.py` —— LRU/FIFO 双策略**（最精巧的抽检点）
   - 一个 `OrderedDict` 同时实现两种策略：LRU 命中时 `move_to_end`，FIFO 命中不动；淘汰统一 `popitem(last=False)`，对 LRU 是"最久未访问"、对 FIFO 是"最早进入"。
   - `set_policy` 切换策略**只改标志、不重建缓存**——一句话能讲清，老师会印象深刻。

2. **`storage/page.py` —— 页模型**
   - 固定 4KB，不足自动补零保证物理页恒为 4KB；`__slots__` 节省内存；页 0 保留给页表（`META_PAGE_ID`）。

3. **`engine/storage_engine.py` —— 槽页布局与行编码**
   - 页头 `>HHHH`（槽数/空闲区起点/空闲长度/标志）+ 槽数组 + 行数据从页尾写入。
   - 删除用 tombstone（偏移 `0xFFFF`）标记、整页全删后回收页——能讲"删除不立即物理清理"的取舍。
   - 行编码自描述 tag：INT `0x00` / FLOAT `0x01` / VARCHAR `0x02` / NULL `0x03`。

4. **`engine/executor.py` + `sql_compiler/planner.py` —— SQL 三值逻辑**
   - NULL 参与运算结果为 unknown（None），Filter 只保留结果为 `True` 的行；常量折叠（`planner.py`）也遵循同一三值逻辑，编译期与运行期语义一致（有 `test_optimizer` 幂等性与保真测试背书）。

5. **错误处理约定**：统一 `[错误类型, 行:列, 原因]` 格式，词法/语法/语义/引擎错误全部不崩溃（Web 层转 400 而非 500）——对应验收准则"所有非法输入均不崩溃"。

6. **工程痕迹**：git 提交 10 次按 P0→P7 阶段演进；`grammar.md` 与实现有对照表；README 中缺陷修复记录 #1~#4（多行插入、孤立分号等，含黑盒回归用例编号）。

**可能的追问与诚实应对**（都是文法内已知限制，主动说明即可）：
- "为什么没有 UPDATE / JOIN / ORDER BY？" → grammar.md §7 明确为扩展规划，本期不实现，不是缺陷。
- "缓存命中率如何验证？" → 用 `db.stats()` / `GET /api/stats` 展示命中率、驱逐、脏页统计。
- 若被问性能 → 如实说明是教学型单机原型，SeqScan + 无索引是设计取舍（SRS 范围外）。

---

## 三、核心亮点（讲什么最加分，按优先级）

1. **全链路贯通、可现场演示**：`python main.py -f demo.sql` 或 Web 控制台直接跑 CREATE → INSERT → SELECT(WHERE) → DELETE，数据落盘后重启仍可查询（TC-E2E-05 持久化）。建议现场演示一遍，胜过讲 10 页 PPT。

2. **编译器四阶段 + 常量折叠优化**：`optimize_with_diff()` 能直接打印优化前后的计划树（`[before]` / `[after constant folding]`），把 `WHERE 1+1=2` 折叠成 `TRUE` 给老师看，直观又有说服力。

3. **存储系统对标真实 DBMS**：4KB 页、LRU/FIFO 可切换、脏页 Checkpoint、槽页 + tombstone + 整页回收、**系统目录以"特殊表"持久化**（catalog 本身经存储引擎读写，重启不丢表结构）——这是课程范围内少见的完整闭环。

4. **工程化测试体系（量化数据最有说服力）**：
   - 481 个用例全绿（本次实测）；黑盒测试 253 个（engine 219 / cli 16 / web 18），边界用例占比 29.64% 且有硬断言（`test_dataset_requirement_audit`）；
   - 核心模块覆盖率 >90%；统一 `run_tests()` 入口，`python tests/run_all.py` 一键跑全部；
   - 缺陷修复有编号、有回归用例、有验证，是"工程规范"的直接证据。

5. **Web 控制台**：React + Ant Design，仿 IDE 编辑区（Ctrl+Enter 执行）、结果表格、**缓存命中率环形图**（`/api/stats`）——把底层存储行为可视化，容易引发老师兴趣。

---

## 附：建议的演示流程（3 分钟）

1. `python tests/run_all.py` 或直接报"481 用例全绿"（30 秒）；
2. Web 控制台执行 `demo.sql` 的完整 CRUD，展示结果表格与错误提示（1 分钟）；
3. 用 `planner.optimize_with_diff` 展示常量折叠前后计划树（30 秒）；
4. 打开 `GET /api/stats` 展示缓存命中率，解释 LRU/FIFO 差异（30 秒）；
5. 关闭再重启程序，查询原表数据仍在（持久化，30 秒）。
