# EchoSQL 记录存储引擎与系统目录答辩说明

## 1. 文档目的

本文档用于说明 EchoSQL / MiniDB 项目中“记录存储引擎与系统目录”这一部分的职责、核心文件、实现逻辑、模块调用关系、运行方式和测试方法。

本文档以当前仓库源码为准。设计文档中规划但当前没有完全接入主链路的功能，会明确标注为“部分实现”或“未实现”。

## 2. 分工定位

本部分对应项目需求中的 FR-3.2 和 FR-3.3，位于 SQL 执行器和底层页式存储之间。

```text
SQL 文本
  ↓
词法分析 / 语法分析 / 语义分析 / 计划生成
  ↓
Executor 执行器
  ↓
TableStorage：管理一张表的多个数据页
  ↓
RowPage：管理一个页中的多个槽和多条记录
  ↓
encode_row / decode_row：行值与二进制 payload 之间转换
  ↓
Storage：4KB 页、页缓存、文件读写、页表
  ↓
数据库文件 data/minidb.db
```

本部分解决的问题是：

1. 一条逻辑记录如何编码成可以写入磁盘的字节；
2. 多条变长记录如何组织在一个固定大小的 4KB 页中；
3. 一张表的数据如何分布在多个数据页中；
4. 插入、扫描、删除和整页回收如何实现；
5. 表名、列定义和数据页集合如何保存；
6. 数据库重启后如何恢复表结构和数据页映射。

本部分不负责 SQL 的词法、语法和语义分析，也不负责底层页缓存算法本身。它通过 `Storage` 使用下层页式存储，通过 `Executor` 接收上层执行计划。

## 3. 核心文件总览

| 文件 | 主要职责 | 答辩重点 |
|---|---|---|
| `engine/storage_engine.py` | `RowPage`、`encode_row`、`decode_row`、`TableStorage` | 记录如何组织成页和槽 |
| `engine/catalog_manager.py` | 系统目录加载、保存、建表、删表、目录查询 | 元数据如何持久化 |
| `engine/errors.py` | `EngineError`、`RowTooLarge`、`CatalogCorrupted` | 引擎层错误边界 |
| `engine/executor.py` | 调用记录存储接口完成建表、插入、扫描和删除 | 上下游如何连接 |
| `engine/__init__.py` | `Database` 统一门面、数据库启动和关闭 | 持久化闭环 |
| `storage/__init__.py` | 下层页式存储统一入口 | 页分配、读写、缓存和刷盘 |
| `storage/file_manager.py` | 页文件、页表、空闲页分配和释放 | 数据页从哪里来 |
| `tests/test_engine.py` | 记录层、表页、删除回收和重启测试 | 功能验证 |
| `tests/test_boundary.py` | 行大小、INT 边界、跨页和中文数据测试 | 边界验证 |
| `demo.sql` | 创建表、插入、查询和删除的端到端演示 | 答辩现场运行 |
| `main.py` | CLI 主入口 | 项目启动方式 |

## 4. 整体调用链

一次 `INSERT` 的调用链如下：

```text
用户输入 INSERT SQL
  ↓
Database.execute()
  ↓
Lexer / Parser / Semantic / Planner
  ↓
Executor._exec_insert()
  ↓
CatalogManager.table_storage()
  ↓
TableStorage.insert_row()
  ↓
encode_row()
  ↓
RowPage.decode()
  ↓
RowPage.insert()
  ↓
Storage.mark_dirty() 或 Storage.allocate_page()
  ↓
CatalogManager.save()（只有页集合发生变化时必须执行）
```

一次 `SELECT` 的调用链如下：

```text
用户输入 SELECT SQL
  ↓
Database.execute()
  ↓
生成 Project / Filter / SeqScan 执行计划
  ↓
Executor._exec_select() / _exec_scan()
  ↓
TableStorage.scan()
  ↓
Storage.get_page()
  ↓
RowPage.decode()
  ↓
RowPage.get()
  ↓
decode_row()
  ↓
Executor 组装查询结果
```

## 5. RowPage：槽页结构

### 5.1 设计原因

一张表中的行可能长度不同。例如：

```text
[1, "Alice", 20]
[2, "Bob", 17]
[3, "一段较长的中文数据", 22]
```

如果直接把行连续排列在页中，就很难根据行号定位变长记录，删除时也容易因为搬移数据而导致其他记录位置发生变化。

因此项目采用 Slotted Page，也就是槽页。槽页使用一个槽数组记录每条行数据在页内的偏移和长度，行数据本身从页尾向前写入。

### 5.2 页内布局

每个页固定为 4096 字节，布局如下：

```text
偏移 0
  ┌──────────────────────────────────────┐
  │ 页头 8 字节                           │
  │ slot_count / free_off / free_len / flags │
  ├──────────────────────────────────────┤
  │ 槽 0：offset + length                 │
  │ 槽 1：offset + length                 │
  │ 槽 2：offset + length                 │
  ├──────────────────────────────────────┤
  │ 空闲区                                │
  ├──────────────────────────────────────┤
  │ 行数据区：从页尾向前写入              │
  └──────────────────────────────────────┘
偏移 4096
```

代码定义：

```python
_PAGE_HEADER = ">HHHH"
_SLOT_FMT = ">HH"
TOMBSTONE = 0xFFFF
```

其中：

- 页头是 4 个无符号 16 位整数，总共 8 字节；
- 每个槽由 offset 和 length 组成，总共 4 字节；
- `TOMBSTONE` 表示该槽对应的记录已经删除；
- 大于号 `>` 表示使用大端字节序。

### 5.3 插入逻辑

`RowPage.insert(payload)` 的核心逻辑是：

```text
1. 计算行数据长度 + 新槽所需的 4 字节；
2. 计算槽数组和数据区之间的剩余空间；
3. 如果空间不足，返回 None；
4. 如果空间足够，free_off 向前移动；
5. 将 payload 写入页尾方向的数据区；
6. 在槽数组中记录 offset 和 length；
7. 返回新槽号。
```

可用空间的计算逻辑是：

```text
available = free_off - (页头大小 + 已有槽数量 × 槽大小)
```

新插入的行必须同时占用：

```text
行数据长度 + 4 字节槽空间
```

一个空页最多可给一条行数据使用约：

```text
4096 - 8 - 4 = 4084 字节
```

如果当前页放不下，`RowPage.insert()` 返回 `None`，由 `TableStorage` 分配新的数据页。

### 5.4 读取逻辑

`RowPage.get(slot)` 的逻辑是：

```text
1. 检查槽号是否越界；
2. 读取槽中的 offset 和 length；
3. 如果 offset 是 TOMBSTONE，返回 None；
4. 否则从页内 offset 位置读取 length 个字节。
```

返回 `None` 表示该槽已经被删除，扫描逻辑会跳过它。

### 5.5 删除逻辑

`RowPage.delete(slot)` 不立即搬移行数据，而是把槽改为：

```text
(TOMBSTONE, 原来的 length)
```

这种方式有三个好处：

- 删除操作简单；
- 不影响其他槽的编号；
- 不会因为移动记录而使扫描过程失效。

槽数组中的行数据空间暂时不会被回收。只有在整页没有活动记录时，`TableStorage` 才会把整个页归还给下层存储系统。

### 5.6 compact

`RowPage.compact()` 会创建一个新的空页，然后把所有未删除记录按照原顺序重新插入，达到整理碎片的目的。

当前源码提供了 `compact()` 接口，但删除后不会自动触发页内压缩。当前主要实现的是：

```text
整页没有活动记录 → 释放整个页
页内部分记录删除 → 保留 tombstone，碎片等待 compact
```

## 6. 行序列化：encode_row / decode_row

### 6.1 编码格式

一行逻辑上是一个值列表，例如：

```python
[1, 2.5, "Alice", None]
```

项目采用 Tag + Payload 的自描述编码格式：

| 类型 | Tag | Payload |
|---|---:|---|
| INT | `0x00` | 大端 32 位有符号整数，4 字节 |
| FLOAT | `0x01` | 大端双精度浮点数，8 字节 |
| VARCHAR / CHAR | `0x02` | 2 字节长度 + UTF-8 字节 |
| NULL | `0x03` | 无额外 payload |

编码后的记录不需要额外依赖 Python 对象类型，解码器可以根据每个字段前面的 Tag 逐字段恢复原始值。

### 6.2 编码过程

`encode_row(values)` 按列顺序遍历输入值：

```text
None
  → 写入 NULL Tag

bool
  → 按 INT 的 0 或 1 保存

int
  → 检查 32 位范围
  → 写入 INT Tag 和 4 字节整数

float
  → 写入 FLOAT Tag 和 8 字节浮点数

其他值
  → 转成字符串
  → 编码为 UTF-8
  → 写入 STRING Tag、2 字节长度和字符串内容
```

### 6.3 边界检查

编码时有三类限制：

1. INT 必须在 `-2147483648` 到 `2147483647` 范围内；
2. 单个字符串最多 `65535` 字节，因为长度字段是 2 字节；
3. 整行必须能放进单个 4KB 页。

超出限制时抛出 `RowTooLarge`，而不是直接让底层 `struct.pack()` 抛出 `struct.error`。

其中，字符串限制按 UTF-8 字节数计算，不是按 Python 字符数计算。中文字符通常占用多个 UTF-8 字节。

### 6.4 解码过程

`decode_row(data)` 从字节数组的第一个字节开始读取 Tag：

```text
读取 Tag
  ↓
根据 Tag 判断后续 payload 长度
  ↓
检查数据是否截断
  ↓
读取并转换为 Python 值
  ↓
继续读取下一个字段
```

遇到以下情况会抛出 `EngineError`：

- INT payload 不完整；
- FLOAT payload 不完整；
- 字符串长度字段不完整；
- 字符串内容不完整；
- 出现未知 Tag。

### 6.5 为什么使用大端序

项目统一使用大端序，主要是为了定义稳定、明确的跨平台二进制格式。只要编码和解码使用相同的格式，不同机器上的字节序差异就不会影响数据解释。

## 7. TableStorage：单表页集合管理

`TableStorage` 表示一张表的物理存储对象，主要保存：

```python
self.name       # 表名
self.columns    # 列定义
self._pages     # 该表拥有的数据页号列表
```

例如：

```text
student → [17, 18, 19]
```

表示 `student` 表的数据分布在 17、18、19 号页中。

### 7.1 插入行

`TableStorage.insert_row(values)` 的逻辑如下：

```text
1. 调用 encode_row(values) 得到 payload；
2. 按 page_id 顺序遍历已有页；
3. 读取页并 RowPage.decode()；
4. 尝试 RowPage.insert(payload)；
5. 如果成功，编码页、标记脏页并返回 False；
6. 如果所有旧页都放不下，分配新页；
7. 将新页加入 _pages；
8. 将第一条记录写入新页；
9. 返回 True。
```

返回值含义：

```text
False：使用已有页，页集合没有改变
True ：分配了新页，页集合发生改变
```

当返回 `True` 时，上层必须保存系统目录，否则重启后虽然新数据页存在，但目录中没有这个页号，扫描时就找不到新页。

### 7.2 顺序扫描

`TableStorage.scan()` 按页号顺序扫描：

```text
for 每一个 page_id:
    从 Storage 获取页
    RowPage.decode()
    for 每一个 slot:
        RowPage.get(slot)
        tombstone → 跳过
        decode_row(raw)
        检查字段数量是否与表定义一致
        yield (page_id, slot, values)
```

扫描结果包含物理行标识：

```python
(page_id, slot, values)
```

例如：

```python
(17, 0, [1, "Alice", 20])
```

执行器会把 `values` 和列定义组合成字典，再用于 `SELECT`、`WHERE` 和 `DELETE`。

### 7.3 删除行

`TableStorage.delete_row(page_id, slot)` 的逻辑如下：

```text
1. 读取目标页；
2. 解码为 RowPage；
3. 把目标槽标记为 tombstone；
4. 如果该页仍有活动记录：
      编码后写回；
      标记为 dirty；
5. 如果活动记录数变成 0：
      从缓存中失效该页；
      释放底层页；
      从 _pages 移除该页。
```

注意：SQL 的批量 `DELETE` 主路径在 `Executor._exec_delete()` 中完成。执行器会扫描多个页，对满足条件的记录进行 tombstone 标记，并在整页为空时执行页回收。`TableStorage.delete_row()` 是底层单行删除接口。

### 7.4 删除整张表

`release_all()` 会释放该表的所有数据页，并清空 `_pages`。它被 `CatalogManager.drop_table()` 用于实现 DROP TABLE 的空间回收。

## 8. CatalogManager：系统目录

### 8.1 系统目录保存什么

系统目录保存每张表的：

```text
表名
列名
列类型
数据页号列表
```

例如：

```json
{
  "tables": [
    {
      "name": "student",
      "columns": [
        {"name": "id", "type": "INT"},
        {"name": "name", "type": "VARCHAR"},
        {"name": "age", "type": "INT"}
      ],
      "pages": [17, 18]
    }
  ]
}
```

### 8.2 目录页的分区

数据库页号大致划分为：

```text
页 0       Storage 页表
页 1~16    系统目录保留区
页 17 起   用户表数据页
```

目录常量定义为：

```python
DIR_START_PAGE = 1
DIR_PAGES = 16
```

目录区域固定为 16 页，总容量约为：

```text
16 × 4096 = 65536 字节，约 64KB
```

目录页由 `CatalogManager._ensure_dir_region()` 通过底层 `allocate_new_page()` 连续预分配，普通用户表不会占用这段区域。

### 8.3 保存目录

`CatalogManager.save()` 的流程：

```text
1. 遍历内存中的所有 TableStorage；
2. 调用 to_json() 得到每张表的元数据；
3. 组织成 {"tables": [...]}；
4. 使用 json.dumps() 编码为 UTF-8；
5. 在 JSON 前写入 4 字节 dir_size；
6. 按 4096 字节切分；
7. 写入页 1、页 2、页 3……。
```

第一个目录页前 4 字节记录 JSON blob 的长度。加载时通过这个长度计算需要读取多少个目录页。

### 8.4 加载目录

`CatalogManager.load()` 的流程：

```text
1. 确保页 1~16 已分配；
2. 读取页 1 的前 4 字节；
3. 得到 dir_size；
4. 计算所需目录页数；
5. 读取目录页并拼接字节；
6. 截取有效 JSON blob；
7. 解析 JSON；
8. 为每张表创建 TableStorage；
9. 恢复列定义和 page_ids；
10. 注册到编译期 Catalog。
```

### 8.5 两个 Catalog 的区别

项目中有两个容易混淆的目录：

| 项目 | 编译期 `sql_compiler.Catalog` | 运行时 `CatalogManager` |
|---|---|---|
| 生命周期 | 一次编译或执行过程 | 数据库整个生命周期 |
| 保存内容 | 表名和列定义 | 表名、列定义和数据页集合 |
| 是否持久化 | 否 | 是 |
| 主要用途 | 语义分析、计划生成 | 恢复和管理真实数据 |
| 数据结构 | 编译器的内存对象 | `TableStorage` 字典加磁盘目录 |

`CatalogManager` 内部同时维护：

```python
self._catalog  # 编译期目录视图
self._tables   # 表名 → TableStorage
```

## 9. 持久化闭环

### 9.1 启动

```text
Database()
  ↓
创建 Storage
  ↓
创建 CatalogManager
  ↓
CatalogManager.load()
  ↓
读取目录页 1~16
  ↓
恢复表结构和数据页列表
  ↓
创建 Executor
```

### 9.2 建表

```text
CREATE TABLE
  ↓
Executor._exec_create()
  ↓
CatalogManager.create_table()
  ↓
创建 TableStorage
  ↓
CatalogManager.save()
```

### 9.3 插入且不扩页

```text
INSERT
  ↓
encode_row()
  ↓
写入已有 RowPage
  ↓
Storage.mark_dirty()
```

此时页集合没有改变，只需要刷写数据页。

### 9.4 插入导致扩页

```text
所有已有页都放不下
  ↓
Storage.allocate_page()
  ↓
TableStorage._pages 增加新页号
  ↓
insert_row() 返回 True
  ↓
CatalogManager.save()
```

目录保存是必要的，因为表到数据页的映射已经发生变化。

### 9.5 删除

```text
DELETE
  ↓
扫描页和槽
  ↓
匹配行标记为 tombstone
  ↓
仍有活动行：写回脏页
没有活动行：释放整个页
  ↓
如果页集合改变，保存 CatalogManager
```

### 9.6 关闭和重启

```text
Database.close()
  ↓
CatalogManager.save()
  ↓
Storage.close()
  ↓
刷写脏页和页表
  ↓
程序重启
  ↓
CatalogManager.load()
  ↓
恢复表结构和数据页集合
```

## 10. 错误处理

错误类型定义在 `engine/errors.py` 中。

```text
sql_compiler.errors
  词法、语法、语义错误

storage.errors
  页越界、页未分配、页数据过大等存储错误

engine.errors
  行太大、记录损坏、目录损坏等引擎运行时错误
```

### 10.1 EngineError

引擎运行时错误的基类，输出格式类似：

```text
[EngineError, 原因]
```

### 10.2 RowTooLarge

以下情况抛出 `RowTooLarge`：

- INT 超出 32 位有符号范围；
- 单个字符串超过 65535 字节；
- 整行无法放入一个页。

这样可以避免底层异常直接冒泡到 CLI 或 Web。

### 10.3 CatalogCorrupted

以下情况抛出 `CatalogCorrupted`：

- 目录 blob 超过固定保留区；
- JSON 无法解析；
- 目录顶层结构不符合预期；
- 查询的表没有注册到运行时目录。

## 11. 运行方式

### 11.1 环境自检

在项目根目录执行：

```powershell
Set-Location H:\vs_code\EchoSQL

python --version
python smoke_test.py
python main.py doctor
```

核心数据库代码主要使用 Python 标准库，因此 CLI 核心演示不依赖 pytest 和 Flask。

### 11.2 运行演示脚本

推荐使用全新的临时目录，避免和已有的 `data/minidb.db` 冲突：

```powershell
$runDir = Join-Path $env:TEMP ("EchoSQL-defense-" + [guid]::NewGuid().ToString())

python main.py -f .\demo.sql --data-dir $runDir
```

`demo.sql` 会执行：

```text
创建 student 表
插入 Alice、Bob、Carol
查询年龄大于 18 的学生
删除 Bob
再次查询 student 表
```

预期最终结果中只剩 Alice 和 Carol。

### 11.3 交互式使用

```powershell
$runDir = Join-Path $env:TEMP ("EchoSQL-repl-" + [guid]::NewGuid().ToString())
python main.py --data-dir $runDir
```

进入 `MiniDB>` 后可以输入：

```sql
CREATE TABLE student(id INT, name VARCHAR, age INT);
INSERT INTO student VALUES(1, 'Alice', 20);
INSERT INTO student VALUES(2, 'Bob', 17);
SELECT * FROM student;
SELECT id, name FROM student WHERE age > 18;
DELETE FROM student WHERE id = 2;
SELECT * FROM student;
```

CLI 内置命令不需要分号：

```text
tables
stats
quit
```

### 11.4 重启验证

退出 REPL 后，用同一个 `$runDir` 重新启动：

```powershell
python main.py --data-dir $runDir
```

输入：

```text
tables
SELECT * FROM student;
stats
quit
```

如果仍然能看到表结构和未删除的数据，说明目录和数据页都已经成功持久化。

### 11.5 测试

当前环境如果没有 pytest，可以创建虚拟环境：

```powershell
py -3.13 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r .\requirements.txt
```

运行记录层测试：

```powershell
.\.venv\Scripts\python.exe -m pytest .\tests\test_engine.py -q
```

运行边界测试：

```powershell
.\.venv\Scripts\python.exe -m pytest .\tests\test_boundary.py -q
```

运行全部测试：

```powershell
.\.venv\Scripts\python.exe -m pytest .\tests -q
```

或者：

```powershell
.\.venv\Scripts\python.exe .\tests\run_all.py
```

### 11.6 Web

安装依赖后运行：

```powershell
.\.venv\Scripts\python.exe -m web.app
```

浏览器访问：

```text
http://127.0.0.1:5000
```

主要接口：

```text
GET  /api/health
POST /api/sql
GET  /api/tables
GET  /api/stats
```

当前 Web 代码默认使用项目的 `data/` 目录，不能像 CLI 一样直接通过参数传入自定义数据目录。

## 12. 测试与功能对应关系

| 测试函数 | 验证内容 |
|---|---|
| `test_rowpage_insert_get_delete` | 槽页插入、读取和删除标记 |
| `test_rowpage_encode_decode_roundtrip` | 槽页编码和解码 |
| `test_rowpage_full_and_compact` | 页满、tombstone 和 compact |
| `test_row_serialization_roundtrip` | INT、FLOAT、字符串、NULL 编解码 |
| `test_row_too_large` | 超大行被拒绝 |
| `test_table_storage_insert_extends_pages` | 插入大量数据时自动扩页 |
| `test_table_storage_delete_releases_page` | 删除整页数据后回收页 |
| `test_drop_table_releases_pages` | 删除整张表并释放全部数据页 |
| `test_e2e_persistence_restart` | 关闭后重启，表和数据仍存在 |
| `test_e2e_persistence_after_delete` | 删除后重启，剩余数据正确 |
| `test_row_size_upper_bound_single_varchar` | 行大小上限 |
| `test_string_value_hard_limit_65535` | 字符串长度字段上限 |
| `test_int_32bit_range_boundary` | INT 范围和溢出处理 |
| `test_bulk_rows_span_multiple_pages` | 多行跨页存储 |
| `test_chinese_and_escaped_string_roundtrip` | 中文和转义字符串 |

## 13. 当前实现状态和限制

### 已实现并已验证的核心功能

- RowPage 槽页结构；
- 行的 Tag + Payload 编码；
- INT、FLOAT、字符串和 NULL 的基本存储；
- 单行大小和 INT 范围检查；
- TableStorage 跨页插入；
- 顺序扫描；
- tombstone 删除；
- 整页回收；
- 系统目录保存和加载；
- 正常关闭后的重启恢复；
- CLI 和 `demo.sql` 端到端运行。

### 部分实现或未完成的功能

- `compact()` 已提供接口，但删除后不会自动执行；
- SQL 批量 DELETE 在 Executor 中直接处理，未完全复用 `TableStorage.delete_row()`；
- 当前没有 B+ 树或哈希索引，查询主要是顺序扫描；
- 当前没有完整事务、WAL 和崩溃恢复机制；
- 目录区固定为 16 页，目录容量有限；
- Web 依赖当前环境没有安装，且 Web 默认使用固定 `data/` 目录；
- pytest 依赖未安装时无法执行自动化测试。

答辩时应按实际完成状态表述，不要把设计文档中尚未接入主链路的功能说成已经完全实现。

推荐表述：

> 当前版本已经打通了记录编码、槽页组织、表页集合管理、系统目录持久化和正常重启恢复这条主链路。自动页内压缩、索引、事务日志和完整崩溃恢复属于尚未完成或后续扩展功能。

## 14. 答辩常见问题

### Q1：为什么使用槽页？

因为行是变长记录。槽数组记录每行的 offset 和 length，可以在一页内定位不同长度的行。删除时只修改槽状态，不需要立即搬移其他行。

### Q2：为什么删除不立即回收空间？

立即搬移可能导致扫描过程失效。先使用 tombstone 可以保证扫描安全跳过被删除行；如果整页没有活动行，再释放整个页。

### Q3：一行如何变成字节？

`encode_row()` 按列顺序为每个值写入类型 Tag，再写入对应 payload。INT、FLOAT、字符串和 NULL 分别采用不同格式。读取时由 `decode_row()` 按 Tag 反向解析。

### Q4：一页满了之后怎么办？

`RowPage.insert()` 返回 `None`，`TableStorage` 就申请新页，把新页号加入 `_pages`，并返回页集合发生变化的标志。执行器随后保存系统目录。

### Q5：系统目录为什么也放在页式存储中？

这样目录和普通数据使用同一套页读写、缓存和刷盘机制，不需要再设计一套独立的元数据文件格式。目录逻辑上是特殊元数据区域，实际通过 `Storage` 的页接口读写。

### Q6：为什么有两个 Catalog？

编译期 Catalog 供语义分析和计划生成使用，只保存表和列信息；运行时 CatalogManager 还保存数据页集合，并负责持久化和重启恢复。

### Q7：当前有没有索引？

没有。当前版本主要使用顺序扫描，重点是验证 SQL 到页式存储的完整链路。索引属于后续扩展方向。

### Q8：当前有没有事务和崩溃恢复？

当前实现了正常关闭时的目录保存、脏页刷盘和页表持久化，但没有完整 WAL、事务回滚和崩溃恢复协议。

## 15. 一分钟总结

> 我负责的是数据库的记录层。底层 Storage 只提供固定大小的 4KB 页，我在它上面实现了 RowPage 槽页，用槽数组管理页内的变长记录；通过 encode_row 和 decode_row 实现逻辑行和二进制数据之间的转换；通过 TableStorage 管理一张表的多个数据页，实现插入时自动扩页、顺序扫描、删除标记和整页回收；通过 CatalogManager 保存表名、列定义以及表到数据页的映射，使数据库关闭后重启仍然能够恢复数据。上层 Executor 负责调用这些接口，下层 Storage 负责缓存、页分配和文件持久化。当前核心链路已经能够运行，索引、事务日志和自动 compact 等增强功能还没有完全实现。
