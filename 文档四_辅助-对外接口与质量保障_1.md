# 文档四（辅助功能三）：对外接口与质量保障体系
## —— CLI 命令行、Flask Web 控制台/REST API、分层测试与黑盒数据集、工程支撑

> 答辩分工：本文档是**辅助文档**，负责系统「**怎么被人用**」与「**怎么被验证是对的**」两部分，并汇总贯穿全系统的错误体系与工程支撑文件。主链路内部原理见文档一，物理存储见文档二、三。
>
> **负责的源码模块**：
> - 接口：`cli/__init__.py`、`main.py`、`web/app.py`、`web/templates/index.html`；
> - 测试：`tests/` 全部（12 个白盒测试模块 + 统一启动器 + P6 黑盒数据集与驱动器）；
> - 工程支撑：`requirements.txt`、`smoke_test.py`、`demo.sql`、`grammar.md`、`data/`、`utils/`。

---

## 1. 三种使用方式总览

系统对用户暴露三条等价入口，背后都是同一套 `Database` 主链路（文档一）：

| 入口 | 启动方式 | 交互形态 | 对应需求 |
| --- | --- | --- | --- |
| 交互式命令行 REPL | `python main.py`（或 `python -m cli`） | `MiniDB>` 提示符，逐行输入 SQL | FR-3.4 / SRS 5.1 |
| 脚本批处理 | `python main.py -f demo.sql` | 一次执行整个 `.sql` 文件并逐条回显 | FR-3.4 |
| Web 控制台 / REST API | `python -m web.app` → http://127.0.0.1:5000 | 网页 SQL 编辑框 + JSON 接口 | SRS 2.3 |

公共启动参数：`--data-dir`（数据目录，默认 `data`）、`--policy LRU/FIFO`（缓存替换策略，文档二）、`--capacity`（缓存页数，默认 8）。

---

## 2. 命令行接口（cli/__init__.py + main.py）

### 2.1 交互式 Shell：MiniDBShell

基于标准库 `cmd.Cmd` 实现，提示符 `MiniDB> `，进入时打印支持语句与辅助命令说明。

- **SQL 执行走 `default()`**：任何不是内建命令的输入都当作 SQL，调用 `db.execute_script()`（文档一的语句切分），对每条结果调用 `result.format()` 打印——SELECT 打印对齐表格，其他打印 message；`SQLError/EngineError` 被捕获后直接 `print`，**输入错误只报错、不退出、不崩溃**。空行由 `emptyline()` 忽略。
- **内建辅助命令**：
  - `tables`：列出全部表及其列定义（`db.table_infos()`）；
  - `stats`：打印缓存命中统计等运行状态（贯通文档二的 `BufferPool.stats`）；
  - `quit / exit / q`：打印 bye 并结束循环。
- **退出兜底**：重写 `cmdloop()`，用 `try/finally` 保证无论正常退出还是 Ctrl-C / EOF，都执行 `db.close()` 完成刷盘持久化。

### 2.2 脚本批处理 run_sql_file

读取整个文件 → `execute_script` 逐条执行并回显；文件打不开返回退出码 1；SQL/引擎错误打印后返回 1；`finally` 中一定 `db.close()`；成功结尾打印 `-- 文件名: N statement(s) executed`，返回 0（退出码可被脚本/CI 判断）。

### 2.3 主入口 main.py

- 用 `argparse` 解析：子命令 `doctor`（环境自检，打印 Python/Flask/pytest/graphviz 版本）与 `-f/--data-dir/--policy/--capacity`；
- 装配出 `Database`，有 `-f` 走批处理、否则进入 REPL；`cli.main()` 也提供了一套等价的 argparse，可 `python -m cli` 独立运行。

---

## 3. Web 控制台与 REST API（web/）

### 3.1 后端 app.py（Flask）

- **应用工厂 `create_app()`**（便于测试创建客户端）；`get_db()` 返回**模块级共享单例** `Database(data_dir="data")`，因此多次 HTTP 请求操作的是同一个持久化数据库，重启进程后数据仍在。
- 五个路由：

| 方法/路径 | 功能 | 返回 |
| --- | --- | --- |
| `GET /` | 渲染 Web 控制台页面（注入环境信息） | HTML |
| `GET /api/health` | 健康检查（服务状态 + Python/平台/Flask/pytest 版本） | JSON |
| `POST /api/sql` | body `{"sql": "..."}` 执行 SQL | 成功 `{ok:true, ...结果}`；失败 **HTTP 400** + 结构化错误 |
| `GET /api/tables` | 全部表及结构（系统目录查询） | JSON |
| `GET /api/stats` | 缓存命中率等运行统计 | JSON |

- **错误到 HTTP 的映射**：`/api/sql` 分别捕获 `SQLError`（返回 kind、`[行,列]` position、错误文本）与 `EngineError`，统一以 **400** 返回（而不是 500）；空 SQL/仅分号返回 `{ok:true, kind:"EMPTY"}`；请求体不是合法 JSON 时 `get_json(silent=True)` 容错为空。
- 开发服务器绑定 `127.0.0.1:5000`、**显式 `debug=False`**：避免 Flask 重载器双开进程争用同一个数据文件。

### 3.2 前端 index.html（单页控制台，无第三方框架）

- SQL 多行输入框 + 「执行」按钮，`fetch('/api/sql')` 提交；SELECT 用 `renderTable` 动态渲染结果表，其他语句显示 message，错误红色显示；执行后自动刷新表列表（`/api/tables`）。
- 支持 **Ctrl/Cmd + Enter** 快捷执行；首页还展示运行环境表。
- **前端做了 XSS 防护**：所有来自后端的数据都经 `escapeHtml` 转义 `& < > " '` 再插入 DOM。

---

## 4. 测试与质量保障体系（tests/）

测试是本项目的重点交付（P5 测试优化、P6 黑盒测试），分「白盒分层测试 + 统一运行框架 + 数据驱动黑盒测试」三部分。

### 4.1 白盒分层测试（12 个模块，静态统计 215 个测试函数）

按系统分层组织，与文档一~三的模块一一对应：

| 测试模块 | 测试函数数 | 覆盖对象 |
| --- | --- | --- |
| test_lexer.py | 14 | 词法：Token 流与位置、关键字大小写、字符串转义、多字符运算符、各类词法错误、三种浮点形式 |
| test_parser.py | 15 | 语法：四类语句 AST、AND/OR/NOT 优先级、括号改结合、缺分号/括号不匹配、空程序 |
| test_semantic.py | 13 | 语义：未知表/列、列数不符、类型不符、运算符类型、重表、WHERE 必须布尔、NULL 通用 |
| test_planner.py | 16 | 计划：SELECT 三段结构、常量折叠、`*`展开、INSERT 列补全、多语句多计划 |
| test_pipeline.py | 6 | 全流水线各阶段产物、错误分别落在词法/语法/语义阶段、坏输入不崩溃 |
| test_storage.py | 28 | 页式存储全部需求（详见文档二第 8 节） |
| test_engine.py | 44 | 引擎端到端 + 记录层（详见文档一/三） |
| test_boundary.py | 34 | **P5 新增**边界/系统测试 |
| test_optimizer.py | 20 | **P5 新增**规则优化专项 |
| test_cli.py | 16 | **P5 新增**命令行系统测试（此前 cli 覆盖为 0） |
| test_web_api.py | 5 | Web API：health、建表→插入→查询、错误 400、tables、空 SQL |
| test_env.py | 4 | 环境：Python 版本、Flask 可用、Web health、冒烟脚本 |
| **合计** | **215** | 部分用例经 `@parametrize` 展开，实际收集条数更多（README 记录 P5 为 222） |

**P5 三块新增测试的重点**：
- `test_boundary`（34）：超长标识符/多语句不崩溃、字符串与数字与注释边界、深层括号/长 OR 链、缓存容量与策略参数校验、页大小与行大小上限、INT 32 位范围、300 行跨页、非 JSON 请求、INT 溢出 Web 返回 400 等；
- `test_optimizer`（20）：常量折叠全规则（算术/七种比较/逻辑三值全值表/NOT/NULL）、部分折叠、优化幂等、无谓词计划不变，以及**「优化前后在同一数据上查询结果必须一致」的引擎级语义保真**；
- `test_cli`（16）：Shell 表格/message/错误打印、tables/stats/quit、外部注入 db 不重复关闭、`run_sql_file` 成功/缺文件/SQL 错误/静默、`main` 参数与非法策略拒绝。

### 4.2 统一启动框架（runner.py / run_all.py / conftest.py）

P5 起每个 `test_*.py` 都提供**同名统一方法** `run_tests(verbose=True, extra_args=None) -> int`（返回 pytest 退出码，0=全过），三种等价运行方式：

```bash
python tests/test_engine.py                              # 直接跑单个模块
python tests/run_all.py                                  # 一键跑全部（等价 pytest tests）
python tests/run_all.py --cov=. --cov-report=term-missing# 总入口透传覆盖率参数
pytest -v                                                # 标准 pytest 亦可
```

- `runner.py`：`discover_test_files` 自动发现 `test_*.py`（辅助文件天然不被收集）、`run_module` 跑单文件、`run_all` 跑全部；**pytest 延迟到函数体内导入**，普通 import 零副作用；
- `run_all.py`：把 tests 目录加入 sys.path 后调用 `run_all`，无参数默认 `-v`、有参数完全透传；
- `conftest.py`：把项目根加入 sys.path，保证直接敲 `pytest` 也能导入 `sql_compiler/storage/engine` 包；
- 覆盖率用 `pytest-cov`，核心模块覆盖率 >90%，并把原本 0% 覆盖的 `cli` 补到全绿。

### 4.3 P6 数据驱动黑盒测试（blackbox_dataset.py + test_blackbox.py）

与白盒相对，P6 完全从**使用者视角**验证，**不引用任何内部模块结构**。

- **数据集 `blackbox_dataset.py`（239 条用例）**：每条用例是一条 dict 记录（构造器 `TC(...)` 并校验编号唯一），字段包括 `id(BB-xxx)/layer/category/ref(需求追溯)/title/boundary(是否边界)/setup(前置语句)/action(被测动作)/expect(期望)/mode`。期望值全部依据 SRS、grammar.md、TC-E2E 与**可观察行为**给出。
- **三层黑盒入口（驱动器 test_blackbox.py）**：
  - `engine` 层（210 条）：只通过 `Database` 门面——SQL 文本进、结果/错误出，含 `sql/script/persist(重启)` 三种模式；
  - `cli` 层（13 条）：用 **subprocess 子进程**真正运行 `python main.py`，只断言进程退出码与标准输出文本；
  - `web` 层（16 条）：用 Flask 测试客户端打 REST API，只断言 HTTP 状态码与 JSON。
  - 通用断言：浮点用 1e-9 容差、None 精确匹配、JSON 子集递归比较。
- **规模硬断言（可机器持续审计）**：`test_dataset_requirement_audit` 强制要求**总用例 ≥ 200 且边界用例占比 ≥ 5%**，不满足整套黑盒测试失败。实际：**239 条、边界 67 条、占比 28.03%**，远超阈值。
- **13 个用例类别**：建表 12 / 插入 30 / 查询 46 / 删除 16 / NULL 12 / 名称大小写 12 / 词法字面量 22 / 表达式 18 / 脚本批处理 10 / 目录持久化 12 / 边界 20 / CLI 13 / Web 16；可 `python tests/test_blackbox.py --report <路径>` 生成 Markdown 报告（仓库内已附 `blackbox_dataset_report.md`，含全部 239 条清单与 67 条边界清单及需求追溯）。

### 4.4 白盒 vs 黑盒对比（答辩常问）

| 维度 | 白盒测试（4.1/4.2） | 黑盒测试（4.3） |
| --- | --- | --- |
| 视角 | 知道内部结构，直接调函数/类 | 只用对外入口（门面/子进程/HTTP） |
| 依据 | 模块实现与分支覆盖 | 需求规格与可观察输入输出 |
| 组织 | 按层分文件、断言内部状态 | 数据驱动，用例与脚本分离 |
| 价值 | 定位具体模块缺陷、覆盖率 | 从用户角度证明整体行为符合需求 |

---

## 5. 缺陷修复记录与已知限制（P5 调试成果）

### 5.1 已修复缺陷（有测试回归）

| 编号 | 缺陷现象 | 修复 | 验证用例 |
| --- | --- | --- | --- |
| #1 | INT 字面量超 32 位时底层 `struct.pack` 抛 `struct.error`，CLI/Web 无法统一捕获、Web 直接 500 | 在 `encode_row` 增加 32 位范围检查，改抛 `RowTooLarge`（文档三） | test_boundary 的 int 32 位边界 / API 溢出返回 400；黑盒 BB-B025/BB-M009 |
| #2 | grammar.md 承诺浮点 `1.`、`.5` 合法，词法器却拒绝 | `_lex_number` 支持 `3.14/1./.5` 三种形式，同时仍拒绝 `1.2.3/.5.6/12abc` | test_boundary 浮点形式/非法浮点；黑盒 BB-B009、BB-G007/008 |

### 5.2 已知限制（文法内不承诺，不算缺陷，答辩应主动说明）

- 无一元负号：`-1` 不能作为字面量（可用 `0-1` 之类表达式规避）；
- 不支持科学计数法（`1e3` 报 LexError）；
- 孤立分号在语法层拒绝，但在 `Database.execute` 层被宽容当作空输入；
- 文法扩展规划中尚未实现：`UPDATE/ORDER BY/GROUP BY/JOIN/DISTINCT`、聚合函数、列别名、乘除取模、长度参数 `VARCHAR(32)`（见 grammar.md §7）。

---

## 6. 贯穿全系统的错误处理体系（汇总）

全系统遵循「**非法输入不崩溃、错误分层、格式统一、就近抛出、边界捕获展示**」：

| 层 | 基类/格式 | 具体类型 | 在哪被转成用户可见输出 |
| --- | --- | --- | --- |
| 编译器（文档一） | `SQLError`，`[类型, 行:列, 原因]` | LexError / ParseError / UnknownTable / UnknownColumn / TableAlreadyExists / ColumnCountMismatch / TypeMismatch | CLI 打印；Web 400 |
| 存储（文档二） | `StorageError`，`[类型] 原因` | InvalidPageId / PageOutOfRange / PageNotAllocated / PageAlreadyAllocated / DataTooLarge / PageTableFull | 由引擎包装/上抛 |
| 引擎（文档三） | `EngineError`，`[类型, 原因]` | RowTooLarge / CatalogCorrupted | CLI 打印；Web 400 |

三层错误都继承 `Exception`、可被精确捕获，因此 REPL 遇错继续、批处理返回非零退出码、Web 返回规范 400 JSON，而**绝不向用户抛堆栈**。

---

## 7. 工程支撑文件与技术栈

### 7.1 目录中其余文件的作用

- `grammar.md`：**SQL 子集文法的权威定义**（词法/语句/表达式文法、类型系统、关键字表、与实现的对照、扩展规划），编译器实现严格与之对齐，是评审与测试用例需求追溯的依据；
- `demo.sql`：端到端演示脚本（建表→插 3 行→条件查询→删除→再查询），`python main.py -f demo.sql` 一键演示主链路；
- `smoke_test.py`：最小环境冒烟（打印 Python 版本与 Smoke test OK）；
- `requirements.txt`：**核心功能只用 Python 标准库**（手写编译器 + struct 文件存储）；额外依赖仅 pytest/pytest-cov（测试）、flask（Web）、graphviz（可选 AST/计划树可视化）；
- `data/minidb.db`：页式存储落盘文件（页 0 页表 + 页 1~16 目录区 + 用户数据页）；
- `utils/__init__.py`：预留通用工具位（当前为空占位）。

### 7.2 技术栈速览

| 层 | 技术 |
| --- | --- |
| 语言 | Python 3.9+（标准库实现编译器与页式存储，零重型依赖） |
| 编译器 | 手写递归下降（Lexer/Parser），词法→语法→语义→计划 |
| 存储 | 文件 I/O + `struct` + 位图页表，4KB 页、LRU/FIFO、Checkpoint |
| Web | Flask（控制台 + REST API） |
| 测试 | pytest + pytest-cov（白盒分层 + 数据驱动黑盒） |
| 可视化 | graphviz（可选） |

### 7.3 现场演示建议（答辩流程）

1. `python main.py doctor` + `python smoke_test.py`：证明环境就绪；
2. `python main.py -f demo.sql`：一次性展示建表/插入/条件查询/删除全链路与对齐表格输出；
3. `python main.py` 进 REPL，手动输入错误 SQL（如表不存在、类型不符）展示「报错不崩溃」与 `tables/stats`；
4. `python -m web.app`，浏览器在控制台执行同一套 SQL，展示 REST 结果表与错误返回 400；
5. `python tests/run_all.py`（环境装好 pytest 时）展示全绿与统一启动；用 `tests/blackbox_dataset_report.md` 说明 239 条黑盒、28.03% 边界占比与需求追溯；
6. 删除前先关闭进程、再重开查询，演示重启持久化。

---

## 8. 答辩可能被问到的问题（接口/测试/工程方向）

1. **CLI 和 Web 是两套实现吗？**
   不是。二者都只是前端：CLI 调 `Database.execute_script/execute`、Web 的 `/api/sql` 调同一个 `Database.execute`，编译、执行、存储完全复用，只是结果分别用 `format()`（文本表格）和 `to_dict()`（JSON）呈现。
2. **为什么 Web 错误返回 400 而不是 500？**
   400 表示「客户端的 SQL/数据有问题」，这正是词法/语法/语义/行容量错误的性质；500 表示服务器自身故障。P5 缺陷 #1 就是把原本冒泡成 500 的 INT 溢出统一改造成业务错误并返回 400。
3. **为什么要有统一的 run_tests()/run_all.py？**
   让不懂 pytest 参数的人也能用完全一致的方式运行任意单个测试模块或全部测试，并返回标准退出码以便接入脚本/CI；同时 conftest 保证直接用 pytest 也能正确导包。
4. **黑盒测试和白盒测试差在哪？为什么两者都要？**
   白盒按模块验证内部逻辑与分支、便于定位；黑盒只从门面/子进程/HTTP 三个对外入口观察「输入→输出/错误」，验证需求符合度。两者互补：内部都对≠用户看到的行为对，反之亦然。
5. **239 条黑盒、67 条边界是怎么保证不注水的？**
   用例集中在数据集文件、与驱动器分离，每条都带需求追溯编号；并有一条硬断言用例要求总数 ≥200、边界占比 ≥5%，不达标整套测试直接失败，规模可被机器持续校验。
6. **核心功能依赖哪些第三方库？**
   编译器与页式存储**只用 Python 标准库**；Flask 仅用于 Web、pytest/pytest-cov 仅用于测试、graphviz 仅用于可选可视化，体现了核心的轻量与可独立运行。
