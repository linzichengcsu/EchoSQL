# EchoSQL · MiniDB 软件运行指导手册

> 适用对象：EchoSQL / MiniDB 数据库管理系统
> （中南大学《大型平台软件设计实习》课程设计）
>
> 本文档依据项目当前代码与既有文档（`README.md`、`grammar.md`、`main.py`、
> `cli/`、`web/app.py`、`engine/__init__.py` 等）编写，说明从零开始
> **搭建环境 → 启动软件 → 使用 SQL → 运行测试 → 常见问题** 的完整流程。

---

## 1. 软件概述

EchoSQL（MiniDB）是一个**可在单机运行的教学型关系型数据库原型**：

- 接收 SQL 文本，经 **SQL 编译器**（词法 → 语法 → 语义 → 逻辑执行计划）翻译；
- 由 **数据库引擎** 调用 **页式存储系统**（4KB 页、LRU/FIFO 缓存、Checkpoint 持久化）完成物理读写；
- 贯通「SQL → 执行计划 → 数据页」完整链路。

对外提供三种使用形态：

| 形态 | 入口 | 说明 |
| --- | --- | --- |
| 命令行（CLI） | `python main.py` | 交互式 SQL REPL（`MiniDB>` 提示符） |
| 脚本批处理 | `python main.py -f 脚本.sql` | 一次性执行 SQL 脚本文件 |
| Web 控制台 / API | `python -m web.app` | 浏览器页面执行 SQL + REST API |

---

## 2. 运行环境要求

| 项目 | 要求 |
| --- | --- |
| 操作系统 | Windows / macOS / Linux 均可（本机开发环境为 Windows） |
| Python | **3.9 或更高**（本机 3.9.13；注意 `python` 命令可能指向 3.14，建议用 `py -3.9` 显式指定） |
| 第三方依赖 | `flask>=3.0`（Web 控制台）、`pytest>=7.0`、`pytest-cov>=4.0`（测试）、`graphviz>=0.20`（可选可视化） |
| 说明 | 核心功能（编译器/存储/引擎/CLI）**仅需 Python 标准库**，Flask 与 graphviz 为可选扩展 |

依赖清单见项目根目录 `requirements.txt`。

---

## 3. 环境搭建与安装

以下命令在**项目根目录**（含 `main.py`、`requirements.txt` 的目录）执行。

### 3.1 创建虚拟环境（推荐）

```powershell
# Windows PowerShell
py -3.9 -m venv venv            # 使用 Python 3.9 创建虚拟环境

# macOS / Linux
python3 -m venv venv
```

### 3.2 激活虚拟环境

```powershell
# Windows PowerShell
.\venv\Scripts\activate

# macOS / Linux
source venv/bin/activate
```

激活成功后，命令行提示符前会出现 `(venv)`。

### 3.3 安装依赖

```powershell
pip install -r requirements.txt
# 网络较慢时可加国内镜像：
# pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 4. 环境自检

安装完成后先做两项快速自检：

```powershell
python smoke_test.py            # 冒烟测试：输出 Python version: 3.9.x / Smoke test OK
python main.py doctor           # 环境自检：打印 Python / Flask / pytest / graphviz 版本
```

- `smoke_test.py` 输出 `Smoke test OK` 即表示基本运行环境正常；
- `main.py doctor` 打印各依赖版本，`未安装` 表示对应可选依赖缺失（不影响核心功能）。

---

## 5. 启动与使用

### 5.1 交互式命令行（REPL）

```powershell
python main.py
```

启动后进入 `MiniDB>` 提示符，直接输入 SQL（以 `;` 结尾）即可执行，一次可输入多条语句：

```
MiniDB 数据库管理系统（中南大学《大型平台软件设计实习》）
支持语句：CREATE TABLE / INSERT / SELECT（WHERE）/ DELETE
辅助命令：tables(表列表)  stats(缓存统计)  help   quit/exit
输入 SQL 语句（以 ; 结束），quit 退出。

MiniDB> CREATE TABLE student(id INT, name VARCHAR, age INT);
OK
MiniDB> INSERT INTO student(id, name, age) VALUES(1, 'Alice', 20);
1 row inserted
MiniDB> INSERT INTO student(id, name, age) VALUES(2, 'Bob', 17);
1 row inserted
MiniDB> SELECT id, name FROM student WHERE age > 18;
id  | name
----+-------
1   | Alice
MiniDB> quit
bye
```

REPL 内建辅助命令：

| 命令 | 作用 |
| --- | --- |
| `tables` | 列出全部表及结构，如 `student(id INT, name VARCHAR, age INT)` |
| `stats` | 打印页缓存命中统计等运行状态 |
| `help` | 查看帮助 |
| `quit` / `exit` / `q` | 保存并退出（自动刷盘持久化） |

> 说明：`Ctrl+C` / `Ctrl+D`（EOF）等异常退出也会在退出前自动刷盘，数据不丢失。

### 5.2 批处理执行 SQL 脚本

```powershell
python main.py -f demo.sql      # 执行项目自带的演示脚本
python main.py -f my_script.sql # 执行任意 SQL 脚本
```

- 脚本按 `;` 切分逐条执行并逐条输出结果；
- 全部成功时打印 `-- 文件名: N statement(s) executed`；
- 任一条语句出错即终止并返回退出码 1，**已执行语句的副作用保留**；
- 等价入口：`python -m cli -f demo.sql`。

### 5.3 常用启动参数

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| `-f, --file FILE` | 无 | 执行 SQL 脚本文件后退出（批处理模式） |
| `--data-dir DIR` | `data` | 数据文件目录（页式存储落盘位置） |
| `--policy {LRU,FIFO}` | `LRU` | 页缓存替换策略 |
| `--capacity N` | `8` | 页缓存容量（页数） |
| `doctor` | — | 环境自检子命令 |

示例：

```powershell
python main.py --data-dir tmp --policy FIFO --capacity 16   # 指定数据目录/策略/容量
python -m cli --data-dir tmp --policy FIFO                  # 等价 CLI 入口
```

### 5.4 启动 Web 控制台

```powershell
python -m web.app
```

启动后浏览器访问 **http://127.0.0.1:5000**，可在页面中输入 SQL 并查看结果表格。

- 健康检查：`GET http://127.0.0.1:5000/api/health`
- Web 服务**固定监听 127.0.0.1:5000**，且关闭了 debug 自动重载（debug 重载会双开进程争用数据文件）；
- Web 控制台使用项目根目录 `data/` 作为数据目录，重启后数据保留；
- 退出服务：在终端按 `Ctrl+C`。

### 5.5 REST API

Web 服务同时提供以下 API（SRS 2.3「也可通过 API 调用」）：

| 接口 | 方法 | 作用 |
| --- | --- | --- |
| `/` | GET | Web 控制台页面 |
| `/api/health` | GET | 健康检查（环境与依赖版本） |
| `/api/sql` | POST | 执行 SQL，请求体 `{"sql": "SELECT * FROM t;"}` |
| `/api/tables` | GET | 列出全部表及结构 |
| `/api/stats` | GET | 页缓存命中率等运行统计 |

`/api/sql` 响应示例：

```json
// 成功（SELECT）
{"ok": true, "kind": "SELECT", "columns": ["id", "name"], "rows": [[1, "Alice"]], "rows_affected": 0, "message": null}

// 成功（INSERT）
{"ok": true, "kind": "INSERT", "columns": [], "rows": [], "rows_affected": 1, "message": "1 row inserted"}

// 失败（SQL 错误，HTTP 400）
{"ok": false, "kind": "UnknownColumn", "position": [1, 8], "error": "[UnknownColumn, 1:8, column 'xx' does not exist]"}
```

使用 `curl` 调用示例：

```bash
curl -X POST http://127.0.0.1:5000/api/sql \
  -H "Content-Type: application/json" \
  -d '{"sql": "SELECT id, name FROM student WHERE age > 18;"}'
```

---

## 6. 支持的 SQL 语法（子集）

详细文法见项目根目录 `grammar.md`。以下为可运行的核心子集。

### 6.1 支持语句

```sql
-- 建表
CREATE TABLE 表名(列名 类型, 列名 类型, ...);

-- 插入（可省略列清单，省略时按表定义列序；VALUES 支持多行，逗号分隔）
INSERT INTO 表名(列1, 列2, ...) VALUES(值1, 值2, ...);
INSERT INTO 表名 VALUES(值1, 值2, ...);
INSERT INTO 表名 VALUES(值1, 值2, ...), (值1, 值2, ...), ...;   -- 一行语句插入多行

-- 查询（WHERE 可选）
SELECT 列1, 列2, ... FROM 表名 WHERE 条件;
SELECT * FROM 表名 WHERE 条件;

-- 删除（WHERE 可选）
DELETE FROM 表名 WHERE 条件;
```

- **关键字大小写不敏感**(`SELECT`/`select` 等价);**表名 / 列名大小写敏感**,须与建表时的拼写逐字符一致(`student` 与 `StuDenT` 是两张不同的表);
- 每条语句以 `;` 结束；多条语句可连写，也可放在同一行；
- 末尾缺分号时引擎会宽容补全（REPL 交互时建议仍以 `;` 结束）；
- 空语句（孤立分号 `;`、连续分号 `;;`）一律报语法错误 `[ParseError, ...]`，不会被静默跳过。

### 6.2 数据类型

| 类型 | 说明 |
| --- | --- |
| `INT` | 32 位有符号整数 |
| `FLOAT` | 浮点数 |
| `VARCHAR` | 变长字符串 |
| `CHAR` | 字符（文法支持） |

> 注意：`VARCHAR(32)` 这类带长度参数的形式**暂不支持**，直接写 `VARCHAR`。

### 6.3 表达式（WHERE 条件）

- 比较运算：`=` `<>` `!=` `<` `<=` `>` `>=`
- 逻辑运算：`AND` `OR` `NOT`（优先级：`NOT` > 比较 > `AND` > `OR`，括号可改变结合）
- 算术运算：`+` `-`（暂不支持 `*` `/` 与 `%`）
- 字面量：整数、浮点数、字符串（单引号）、`TRUE` / `FALSE` / `NULL`
- 三值逻辑：与 `NULL` 比较结果为 unknown（既不成立也不排除），语义与常量折叠一致

示例：

```sql
SELECT * FROM student WHERE age > 18 AND name <> 'Bob';
SELECT id FROM student WHERE NOT age = 20 OR (age >= 17 AND age <= 22);
```

### 6.4 注释与转义

```sql
-- 行注释：双减号到行尾
/* 块注释：可跨行 */

-- 字符串内单引号用两个连续单引号转义
INSERT INTO t VALUES('Tom''s book');   -- 存储为 Tom's book
```

### 6.5 错误信息格式

所有错误统一为 `[错误类型, 行:列, 原因]` 文本，**不会导致程序崩溃**：

- 词法/语法/语义错误：`[LexError, 1:8, unexpected character '@']`、`[UnknownColumn, 1:8, column 'score' does not exist]`、`[TableAlreadyExists, ...]` 等；
- 存储错误：`[StorageError, 原因]`；
- 引擎错误：`[EngineError, 原因]`（如 `[RowTooLarge, ...]`，INT 超出 32 位范围时报此错）。

---

## 7. 演示脚本 demo.sql

项目自带的 `demo.sql` 演示完整「建表 → 插入 → 条件查询 → 删除 → 再查询」链路：

```powershell
python main.py -f demo.sql
```

预期输出依次为：建表 OK → 3 行插入 → `age > 18` 查出 Alice、Carol → 删除 Bob → 二次查询 Bob 不可见。

> ⚠️ `demo.sql` 每次执行会**向当前数据目录追加同名表**，若 `student` 表已存在会报
> `TableAlreadyExists`。需要干净演示时，先删除 `data/minidb.db` 或换 `--data-dir`（见第 9 节）。

---

## 8. 自动化测试

### 8.1 一键运行全部测试

```powershell
pytest -v                              # 全部测试（逐用例显示）
pytest -q                              # 静默模式
pytest tests                           # 等价于上两条（tests 目录）
```

### 8.2 统一启动方法（无需了解 pytest 细节）

```powershell
python tests/run_all.py                # 一键运行全部测试（默认 -v，参数透传）
python tests/run_all.py --cov=. --cov-report=term-missing   # 附带覆盖率统计
python tests/test_engine.py            # 直接运行单个测试模块（任意 tests/test_*.py）
```

### 8.3 覆盖率统计

```powershell
pytest tests --cov=. --cov-report=term-missing
```

### 8.4 编程方式调用（可选）

```python
import sys
sys.path.insert(0, "tests")
import test_optimizer as m
sys.exit(m.run_tests(verbose=False))   # 运行 test_optimizer 全部用例，返回 pytest 退出码
```

退出码约定：**0 = 全部通过**。

测试模块一览（`tests/`）：

| 模块 | 类型 | 内容 |
| --- | --- | --- |
| test_lexer / test_parser / test_semantic / test_planner / test_pipeline | 编译器单测 | 词法/语法/语义/计划/全流水线 |
| test_storage | 存储单测 | 页、缓存、替换策略、持久化 |
| test_engine | 引擎 E2E | TC-E2E-01~05 端到端 |
| test_web_api | Web 集成 | REST API |
| test_env | 环境 | 依赖/版本 |
| test_boundary | 边界/系统 | 超长输入、容量边界、INT 32 位范围、跨页大数据等 |
| test_optimizer | 规则优化 | 常量折叠、优化保真 |
| test_cli | CLI 系统 | REPL 输出、批处理、参数解析 |
| test_blackbox | 黑盒 | 数据集黑盒测试（含 `blackbox_dataset.py`） |

---

## 9. 数据持久化与重置

- 数据库文件位于数据目录（默认 `data/`）下的 **`minidb.db`**：
  - 表数据以 **4KB 页** 存储，页 0 保留给页表，文件开头固定区域（页 1~16）存放系统目录（表名/列名/列类型/页集合）；
  - 正常关闭（`quit`、`db.close()`）时自动 **Checkpoint 刷盘**并持久化系统目录；
  - **重启后表结构与数据不丢失**（已验证 TC-E2E-05）。
- 查看运行统计：REPL 中输入 `stats`，或 `GET /api/stats`，可看到缓存命中率、命中/读盘次数等（FR-2.2）。
- **重置/清理演示数据**的三种方式：
  1. 删除 `data/minidb.db` 后重启（数据目录重建）；
  2. 换用新目录：`python main.py --data-dir tmp`；
  3. 用 Python API：`db.drop_table("student")` 删除单表（回收其全部数据页）。

> 注意：多个进程（如 Web 与 CLI）同时打开同一数据目录会争用数据文件，请勿并行操作同一 `data/`。

---

## 10. 常见问题（FAQ）

| 现象 | 原因与解决办法 |
| --- | --- |
| 建表报 `TableAlreadyExists` | 表已存在（`demo.sql` 重复执行也会触发）。删除 `data/minidb.db`、换 `--data-dir`，或用 `drop_table` 删除该表。 |
| `python main.py doctor` 显示依赖 `未安装` | 未执行 `pip install -r requirements.txt`，或未激活 venv。Flask/pytest/graphviz 缺失不影响核心 CLI 功能。 |
| Web 启动失败：端口 5000 被占用 | 关闭占用进程，或修改 `web/app.py` 中 `app.run(...)` 的 `port` 参数。 |
| Web 页面打不开 / 报 TemplateNotFound | 确保在**项目根目录**执行 `python -m web.app`（模板位于 `web/templates/index.html`）。 |
| Web 报 500 而非 400 | 正常情况非法 SQL 返回 400；若出现 500 请确认是否为 INT 溢出等已知缺陷场景（P5 已修复为 400）。 |
| 数据删不掉 / 查询结果不更新 | 检查是否多进程共用同一 `data/`（Web 与 CLI 同时运行会争用文件）。 |
| `python` 指向了 3.14 而非 3.9 | 本机同时装有多个 Python。用 `py -3.9` 显式指定，或重激活 venv（venv 内固定解释器）。 |
| 错误信息形如 `[TypeMismatch, 1:28, ...]` | SQL 类型不匹配，按 `[错误类型, 行:列, 原因]` 定位并修正。 |

---

## 11. 已知限制（文法内不承诺，不视为缺陷）

- **不支持** `UPDATE`、`ORDER BY`、`GROUP BY`、`JOIN`、`DISTINCT`、聚合函数（`COUNT(*)` 等）、列别名（`AS`）；
- 表达式中**不支持乘除**（`*` `/`）与取模（`%`）；
- **无一元负号**：`-1` 不能作为字面量；
- **不支持科学计数法**（`1e3` 报 LexError）；
- 不支持 `VARCHAR(32)` 等带长度参数的类型写法；
- 空语句非法：孤立分号 `;` 与连续分号 `;;` 在语法层拒绝（`Database.execute` / `execute_script` 均抛 ParseError）；仅空串 / 纯空白 / 纯注释按空输入处理；
- 数字后紧跟字母（`12abc`）、浮点再跟小数点（`1.2.3`）视为非法数字，报词法错误。

---

## 12. 项目目录结构速览

```
EchoSQL/
├── main.py            # 主程序入口（REPL / -f 批处理 / doctor 自检）
├── smoke_test.py      # 环境冒烟测试
├── demo.sql           # 端到端演示 SQL 脚本
├── grammar.md         # SQL 子集文法（与编译器实现严格一致）
├── requirements.txt   # 依赖清单
├── README.md          # 项目总览
├── sql_compiler/      # SQL 编译器：lexer / parser / semantic / planner / catalog / ast / errors / tokens
├── storage/           # 存储系统：page / buffer(LRU、FIFO) / file_manager / errors
├── engine/            # 数据库引擎：executor / storage_engine / catalog_manager / errors
├── cli/               # 命令行接口（cmd REPL，-f 批处理）
├── web/               # Flask Web 控制台与 API（templates/index.html）
├── tests/             # pytest 分层测试（runner.py + run_all.py 统一入口）
├── utils/             # 工具函数
├── data/              # 数据文件目录（minidb.db + 引擎系统目录）
├── docs/              # 课程设计文档（SRS / 测试文档 / 技术栈指导 / 本手册）
└── ai/                # 文档抽取辅助脚本
```

---

## 13. 附录：Python 编程接口（可选）

数据库引擎对外暴露统一门面 `engine.Database`，可在脚本中直接使用：

```python
from engine import Database

db = Database(data_dir="data")                 # 打开/创建数据库（自动加载系统目录）
db.execute("CREATE TABLE student(id INT, name VARCHAR, age INT);")
db.execute("INSERT INTO student VALUES(1,'Alice',20);")
result = db.execute("SELECT id, name FROM student WHERE age > 18;")
print(result.format())                         # SELECT 输出对齐表格 / 其余输出 message

db.execute_script("INSERT ...; DELETE ...;")   # 按 ';' 切分逐条执行，返回结果列表
db.tables() / db.table_infos()                 # 目录查询
db.stats()                                     # 缓存命中率等运行统计
db.drop_table("student")                       # 删表并回收全部数据页
db.close()                                     # 关闭前持久化目录 + Checkpoint 刷盘
```

也支持 `with` 语句自动关闭：

```python
with Database(data_dir="data") as db:
    db.execute("SELECT * FROM student;")
```

---

*本手册依据项目现有代码与文档整理，若后续版本变更命令或接口，请以 `README.md` 与各模块 docstring 为准。*
